{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch CodeLLDB",
      "program": "bun",
      "args": ["../src/bin.ts", "commit", "-m", "Initial commit"],
      "cwd": "${workspaceFolder}/client/workspace",
      "env": {
        "NODE_CONFIG_DIR": "${workspaceFolder}/client/config"
      }
    }
  ]
}
