stub:
  enabled: true
  storage-path: "./data/filer"
  external-url: "localhost:13001"

connection:
  master:
    host: "master"
    port: 9333
    tls: false
  filer:
    host: "localhost"
    port: 80
    tls: false

jwt:
  signing-key: "secret"
  system-signing-key: "system-secret"
  expiration-ms: 3000
