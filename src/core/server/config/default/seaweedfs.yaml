stub:
  enabled: true
  storage-path: "./data/filer"

connection:
  master:
    host: "master"
    port: 9333
    tls: false
  filer:
    internal:
      host: "localhost"
      port: "${server.listen-port}"
      tls: false
      path: "/filer"
    external:
      host: "localhost"
      port: "${server.listen-port}"
      tls: false
      path: "/filer"

jwt:
  signing-key: "secret"
  system-signing-key: "system-secret"
  expiration-ms: 3000
