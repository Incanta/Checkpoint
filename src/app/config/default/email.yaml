enabled: false

smtp:
  host: ""
  port: 587
  secure: false # most email providers use STARTTLS on port 587, which is not secure by default but can be upgraded to secure
  auth:
    user: ""
    pass: ""

from:
  name: "Checkpoint VCS"
  address: "noreply@example.com"
