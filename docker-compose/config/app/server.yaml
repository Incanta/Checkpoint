listen-port: 13000

# For a LAN quickstart, change IP_ADDRESS to your host's LAN IP address (e.g. 192.168.1.10).
# In production, this likely should be https://checkpoint.example.com; it's up
# to you to configure TLS termination and reverse proxying if you do that.
external-url: "http://IP_ADDRESS:13000"
